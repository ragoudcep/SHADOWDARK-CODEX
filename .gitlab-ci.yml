pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r $(ls -A | grep -vE '^(public|\.git|\.gitlab-ci\.yml)$') public/
  artifacts:
    paths:
      - public
  pages: true
  rules:
    - if: $CI_COMMIT_BRANCH == "main"